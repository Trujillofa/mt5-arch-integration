# Optional Hyprland snippet for MetaTrader 5 under Wine.
# Include from ~/.config/hypr/hyprland.conf:
#   source = ~/Projects/trading/mt5-arch-integration/ops/hyprland/mt5-window-rules.conf
#
# Goals: keep the main terminal visible; avoid minimize-to-void behavior when possible.

# Float Login dialogs so they stay on top of the main shell
windowrulev2 = float, class:^(terminal64\.exe)$, title:^(Login)$
windowrulev2 = center, class:^(terminal64\.exe)$, title:^(Login)$

# Prefer not suppressing Wine windows
windowrulev2 = noblur, class:^(terminal64\.exe)$

# Note: do NOT force minimize-to-special-workspace — that is how windows "disappear".
# Prefer a dedicated workspace (e.g. Super+Shift+7) instead of the minimize button.
